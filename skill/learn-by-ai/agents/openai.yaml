interface:
  display_name: "Learn by AI"
  short_description: "Reconnect and run adaptive learning projects"
  default_prompt: "Use $learn-by-ai to find the local project matching my named subject before starting. If several projects match, let me choose; then resume its checkpoint, research the unit, teach adaptively, and preserve progress."
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
policy:
  allow_implicit_invocation: true
